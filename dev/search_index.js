var documenterSearchIndex = {"docs":
[{"category":"section","location":"profile_methods/#profile_likelihood_methods","page":"Profile likelihood methods","text":"LikelihoodProfiler provides a range of methods to profile likelihood functions and explore practical identifiability. The method should be provided as the second argument to the solve function.","title":"Profile Likelihood Methods"},{"category":"section","location":"profile_methods/#optimization_based_profiles","page":"Profile likelihood methods","text":"The method computes profiles for each parameter by iteratively changing the value of the parameter and re-optimizing the likelihood function with respect to all other parameters. ","title":"Optimization-based profiles"},{"category":"section","location":"profile_methods/#Optimization-steppers","page":"Profile likelihood methods","text":"OptimizationProfiler uses a stepper to choose the next value along the profiled parameter axis before re-optimizing all remaining parameters.\n\nUse FixedStep when you want a predictable step rule that does not inspect the objective function at trial points:\n\nmethod = OptimizationProfiler(\n\toptimizer = LBFGSB(),\n\tstepper = FixedStep(; initial_step = 0.1),\n)\n\nUse AdaptiveStep when you want the profiler to adjust the step length based on the objective increase observed at trial points:\n\nmethod = OptimizationProfiler(\n\toptimizer = LBFGSB(),\n\tstepper = AdaptiveStep(; initial_step = AdaptiveInitialStep()),\n)\n\nThe default AdaptiveInitialStep scales the first step with the current profiled value and clamps it to configured minimum and maximum bounds. This is useful when parameters have different numerical scales.\n\nWith its default settings, AdaptiveInitialStep() proposes max(0.01 * abs(x), 1e-3) and clamps the result to [1e-4, Inf]. These defaults are conservative for parameters on their original scale. For parameters on a log or otherwise standardized scale, a fixed initial step such as 0.01 can also be a convenient choice.","title":"Optimization steppers"},{"category":"section","location":"profile_methods/#How-adaptive-stepping-works","page":"Profile likelihood methods","text":"After the first profile point, AdaptiveStep separates the proposal into a direction and a length:\n\nThe predictor estimates how all parameters should move when the profiled parameter changes.\nThe controller evaluates the objective at trial points along that direction, without re-optimizing the nuisance parameters.\nThe trial step is increased or decreased until its objective change is close to the upper target, or a profile bound or configured step limit is reached.\nThe nuisance parameters are re-optimized only after the proposal has been selected.\n\nThe trial objective therefore measures the quality of the optimization starting point, not the final profile objective. The final objective will usually be lower after re-optimization, and it is allowed to cross the profile threshold. A threshold crossing terminates that profile branch in the usual way.\n\nThe default LinearPredictor extrapolates the complete parameter vector from the two previous optimized profile points. It is usually the most efficient choice for smooth profiles because it follows the estimated profile path through the nuisance-parameter space. SingleAxisPredictor changes only the profiled parameter and leaves all nuisance parameters at their current optimum. It is more conservative and can be useful for profiles with abrupt turns, local optima, or unreliable extrapolation.\n\n# Default: first-order extrapolation of the complete profile path\nadaptive = AdaptiveStep(; predictor = LinearPredictor())\n\n# Conservative alternative: move only the profiled parameter\nconservative = AdaptiveStep(; predictor = SingleAxisPredictor())\n\nIf adaptive search with LinearPredictor fails or reaches its minimum step, the profiler automatically retries with SingleAxisPredictor. If optimization at an accepted adaptive proposal fails, it is retried once with half the proposed step.","title":"How adaptive stepping works"},{"category":"section","location":"profile_methods/#Objective-step-control","page":"Profile likelihood methods","text":"ObjectiveStepControl defines the desired objective change and the allowed profile-axis step sizes. For a finite profile threshold, its upper target is\n\nDelta f_mathrmtarget =\noperatornameclamp(mathttthreshold_fractiontau\n                     mathttmin_obj_step\n                     mathttmax_obj_step)\n\nwhere tau is the objective threshold relative to the optimum. The default threshold_fraction=0.1 aims for approximately ten profile steps over that objective range when the local profile shape is regular.\n\nWhen threshold=Inf, no confidence threshold is available as an objective scale. The target is instead based on target_factor times the absolute objective change between the two previous optimized profile points, clamped by min_obj_step and max_obj_step.\n\nThe most useful tuning options are:\n\nOption Effect of increasing it\nthreshold_fraction Fewer, larger steps for finite thresholds\nmin_obj_step Larger steps when the objective is flat or threshold=Inf\nstep_factor Faster but coarser trial-step growth and reduction\nmax_x_step_growth Faster growth across flat profile regions\nmin_x_step Prevents excessively dense sampling\nmax_x_step Limits jumps along the profiled parameter axis\n\nFor a profile that remains too dense in flat regions, first increase the initial step or min_obj_step. If growth is still too slow, increase max_x_step_growth. For unstable optimization or a sharply curved profile, reduce threshold_fraction or max_x_step, or select SingleAxisPredictor().\n\ncontroller = ObjectiveStepControl(\n    threshold_fraction = 0.1,\n    min_obj_step = 0.01,\n    min_x_step = 1e-4,\n    max_x_step = 0.5,\n)\n\nmethod = OptimizationProfiler(\n    optimizer = LBFGSB(),\n    stepper = AdaptiveStep(; controller),\n)","title":"Objective step control"},{"category":"section","location":"profile_methods/#integration_based_profiles","page":"Profile likelihood methods","text":"The method computes profiles for each parameter (or function of parameters) by integrating the differential equations system. \n\nReferences:\n\nChen, J.-S. & Jennrich, R. I. Simple Accurate Approximation of Likelihood Profiles. Journal of Computational and Graphical Statistics 11, 714–732 (2002).\nChen, J.-S. & Jennrich, R. I. The Signed Root Deviance Profile and Confidence Intervals in Maximum Likelihood Analysis. Journal of the American Statistical Association 91, 993–998 (1996).","title":"Integration-based profiles"},{"category":"section","location":"profile_methods/#cico_profiles","page":"Profile likelihood methods","text":"The method computes intersections (endpoints of the confidence interval (CI)) of the profile with the predefined confidence level (threshold) without restoring the exact trajectory of the profile. Requires using CICOBase package.\n\nReferences:\n\nBorisov, I. & Metelkin, E. Confidence intervals by constrained optimization—An algorithm and software package for practical identifiability analysis in systems biology. PLoS Comput Biol 16, e1008495 (2020).\nVenzon, D. J. & Moolgavkar, S. H. A Method for Computing Profile-Likelihood-Based Confidence Intervals. Applied Statistics 37, 87 (1988).","title":"Confidence Intervals by Constrained Optimization (CICO)"},{"category":"section","location":"profile_methods/#fim_profiles","page":"Profile likelihood methods","text":"","title":"Quadratic approximation (FIM curvature at optimum)"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.OptimizationProfiler-profile_methods","page":"Profile likelihood methods","text":"OptimizationProfiler{S, opType, optsType}\n\nA profiler method that uses stepwise re-optimization to profile the likelihood function.\n\nFields\n\nstepper::S: The algorithm used to compute the next profile point. Supported steppers include:\nFixedStep: Proposes steps from the configured step rule in the profiling direction (Default). \nAdaptiveStep: Adapts the step size based on trial objective increases.\noptimizer::opType: The optimizer used for the optimization process.\noptimizer_opts::optsType: Options for the optimizer. Defaults to NamedTuple().\n\nStepping Options\n\nThe stepper argument controls how the next profile point is chosen. For example:\n\nstepper = FixedStep(initial_step=0.1): Use a constant step size of 0.1.\nstepper = AdaptiveStep(predictor=LinearPredictor()): Use adaptive stepping with a secant-like linear predictor.\n\nSee the documentation for each stepper type (e.g., ?FixedStep, ?AdaptiveStep) for more details and customization options.\n\nExample\n\nusing OptimizationLBFGSB\nprofiler = OptimizationProfiler(; optimizer = LBFGSB(), optimizer_opts = (reltol=1e-4,))\n\n\n\n\n\n","title":"LikelihoodProfiler.OptimizationProfiler"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.AdaptiveInitialStep-profile_methods","page":"Profile likelihood methods","text":"AdaptiveInitialStep(; rel_step=0.01, abs_step=1e-3, min_step=1e-4, max_step=Inf)\n\nInitial profile step rule that adapts to the current profiled parameter value.\n\nThe proposed step is max(rel_step * abs(x), abs_step), where x is the current profiled parameter value. The result is finally clamped to [min_step, max_step].\n\nThis is useful when parameters have different numerical scales and a single fixed absolute step would be inefficient.\n\n\n\n\n\n","title":"LikelihoodProfiler.AdaptiveInitialStep"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.FixedStep-profile_methods","page":"Profile likelihood methods","text":"FixedStep{S}\n\nProfiler stepper that proposes each profile point from the configured initial_step rule without adapting to trial objective values.\n\nConstructors\n\nFixedStep(;initial_step=AdaptiveInitialStep())\n\nKeyword arguments\n\ninitial_step=AdaptiveInitialStep(): The step rule to use for each profile step. This can be a number (for a constant absolute step size), an AdaptiveInitialStep, or a callable ctx -> step for custom logic depending on the current profiler cache. If a number is provided, it is automatically wrapped as a function.\n\n\n\n\n\n","title":"LikelihoodProfiler.FixedStep"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.AdaptiveStep-profile_methods","page":"Profile likelihood methods","text":"AdaptiveStep(; initial_step=AdaptiveInitialStep(),\n               predictor=LinearPredictor(),\n               controller=ObjectiveStepControl())\n\nProfiler stepper that adapts the profile step length based on the observed objective increase from trial points.\n\nThe first profile step uses initial_step. Later steps use predictor to choose the direction in parameter space and controller to keep the next objective increase in a useful range.\n\n\n\n\n\n","title":"LikelihoodProfiler.AdaptiveStep"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.ObjectiveStepControl-profile_methods","page":"Profile likelihood methods","text":"ObjectiveStepControl(; threshold_fraction=0.1,\n                       target_factor=1.5,\n                       lower_factor=0.25,\n                       min_obj_step=1e-2,\n                       max_obj_step=Inf,\n                       min_x_step=1e-4,\n                       max_x_step=Inf,\n                       step_factor=1.5,\n                       max_x_step_growth=5.0,\n                       maxiters=15)\n\nControls adaptive profile stepping by defining acceptable objective and profile-axis step ranges.\n\nFor finite likelihood thresholds, the target objective increase is based on threshold_fraction * threshold. For infinite thresholds, the target increase is based on target_factor * abs(obj_cur - obj_prev).\n\nThe previous optimized profile objective change is used to nudge the first trial step up or down. Trial points below the upper objective target are then treated as safe to grow until the upper target, profile bound, or iteration limit is reached. The next profile-axis step is additionally capped by max_x_step_growth * previous_step to avoid abrupt jumps in flat profile regions.\n\n\n\n\n\n","title":"LikelihoodProfiler.ObjectiveStepControl"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.LinearPredictor-profile_methods","page":"Profile likelihood methods","text":"LinearPredictor()\n\nExtrapolates all parameters using linear extrapolation based on the last two successful profile points.\n\n\n\n\n\n","title":"LikelihoodProfiler.LinearPredictor"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.SingleAxisPredictor-profile_methods","page":"Profile likelihood methods","text":"SingleAxisPredictor()\n\nExtrapolates only the profiled parameter using linear extrapolation based on the last two successful profile points.\n\n\n\n\n\n","title":"LikelihoodProfiler.SingleAxisPredictor"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.IntegrationProfiler-profile_methods","page":"Profile likelihood methods","text":"IntegrationProfiler{opType, optsType, DEAlg, DEOpts}\n\nA profiler method that uses integration of differential equations system to profile the likelihood function.\n\nFields\n\nreoptimize::Bool: Indicates whether to re-optimization after each step of the integrator. Defaults to false.\noptimizer::opType: The optimizer used for the optimization process. Defaults to nothing.\noptimizer_opts::optsType: Options for the optimizer. Defaults to NamedTuple().\nintegrator::DEAlg: The differential equation algorithm used for integration.\nintegrator_opts::DEOpts: Options for the differential equation solver. Defaults to NamedTuple().\nmatrix_type::Symbol: The type of matrix to be used for the Hessian approximation. Possible options are: :hessian, :identity. Defaults to :hessian.\ngamma::Float64: Correction factor used in integration if full hessian is not computed (e.g. matrix_type = :identity). Defaults to 1.0.\n\nExample\n\nusing OrdinaryDiffEqTsit5\nprofiler = IntegrationProfiler(integrator = Tsit5(), integrator_opts = (dtmax=0.3,), matrix_type = :hessian)\n\n\n\n\n\n","title":"LikelihoodProfiler.IntegrationProfiler"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.CICOProfiler-profile_methods","page":"Profile likelihood methods","text":"CICOProfiler\n\nConfidence Intervals by Constrained Optimization (CICO) method to find the intersections of the likelihood function with the threshold. See CICOBase docs for more details. Requires using CICOBase.\n\nFields\n\noptimizer::Symbol: The optimizer used for the optimization process. Defaults to NLopt :LN_NELDERMEAD.\nscan_tol::Float64: The tolerance for the endpoints scan. Defaults to 1e-3.\n\nExample\n\nprofiler = CICOProfiler(optimizer = :LN_NELDERMEAD, scan_tol = 1e-3)\n\n\n\n\n\n","title":"LikelihoodProfiler.CICOProfiler"},{"category":"type","location":"profile_methods/#LikelihoodProfiler.QuadraticApproxProfiler-profile_methods","page":"Profile likelihood methods","text":"QuadraticApproxProfiler\n\nQuadratic-approximation confidence intervals (Wald approximation) based on local curvature at the optimum. The curvature is approximated by the Fisher Information Matrix/Hessian, so the resulting confidence intervals reflect the local quadratic approximation of the likelihood around optpars. By default this method reuses Hessian logic from OptimizationProblem (user-supplied Hessian or AD backend). The confidence interval is computed as θ̂ ± z * sqrt(Σ[idx, idx]), where          - θ̂ is the optpars[idx],          - z is the quantile of the chi-squared distribution corresponding to the conf_level and df parameters of the ProfileLikelihoodProblem,         - Σ is the covariance matrix obtained by inverting the FIM.\n\ncov_factor controls Hessian/objective scaling conventions. Common choices are:\n\n1.0 when Hessian is for -logL.\n2.0 when Hessian is for -2logL and you want covariance on the -logL scale.\n\nAny strictly positive value is allowed (not only 1 or 2), which can be useful for calibrated or robust variance scaling.\n\nFields\n\ninversion::Symbol: Matrix inversion strategy (:cholesky, :pinv).\nclamp_to_bounds::Bool: Clip estimated interval endpoints to profile bounds.\ncov_factor::Real: Multiplicative factor applied to inv(H) to obtain covariance (Σ = cov_factor * inv(H)).\nresolution::Int: Number of points per branch (left/right) used to sample the quadratic approximation.\n\n\n\n\n\n","title":"LikelihoodProfiler.QuadraticApproxProfiler"},{"category":"section","location":"api/#API-references","page":"API","text":"The package exports the following functions for parameters and functions identifiability analysis, confidence intervals evaluation and results visualization.","title":"API references"},{"category":"method","location":"api/#CommonSolve.solve-Tuple{ProfileLikelihoodProblem, LikelihoodProfiler.AbstractProfilerMethod}","page":"API","text":"solve(plprob::ProfileLikelihoodProblem, method::AbstractProfilerMethod; \n        parallel_type::Symbol=:none, maxiters::Int=1e4, verbose::Bool=false)\n\nProfiles the likelihood function for the given problem plprob using the specified profiling method.\n\nArguments\n\nplprob::ProfileLikelihoodProblem: The profiling problem instance containing the parameters and likelihood function to be profiled.\nmethod::AbstractProfilerMethod: The method to be used for profiling.\nreoptimize_init::Bool=false: If true, re-optimizes the model at the provided initial parameter values optpars before profiling. Defaults to false.\nparallel_type::Symbol: Specifies the type of parallelism to be used. Supported values: :none, :threads, :distributed. Defaults to :none.\nmaxiters::Int: Maximum number of iterations for one branch (left and right) of the profiling process. Defaults to 1e4.\nverbose::Bool: Indicates whether to display the progress of the profiling process. Defaults to false.\n\nReturns\n\nReturns the profiling results ProfileLikelihoodSolution.\n\nExample\n\nplprob = ProfileLikelihoodProblem(optprob, optpars; idxs=1, profile_lower=-10., profile_upper=10.)\nmethod = OptimizationProfiler(optimizer = LBFGSB(), stepper = FixedStep())\nsol = solve(plprob, method)\n\n\n\n\n\n","title":"CommonSolve.solve"},{"category":"method","location":"api/#LikelihoodProfiler.__solve-Tuple{ProfileLikelihoodProblem, LikelihoodProfiler.AbstractProfilerMethod}","page":"API","text":"__solve(plprob::ProfileLikelihoodProblem, method::AbstractProfilerMethod; \n        parallel_type::Symbol=:none, kwargs...)\n\nInternal solve dispatcher used by solve after input validation and optional re-optimization. By default, it constructs parameter profile branches (idx, dir) and forwards them to __solve_parallel. Profilers with a different execution model (e.g. non-branch methods) can overload this function.\n\n\n\n\n\n","title":"LikelihoodProfiler.__solve"},{"category":"function","location":"api/#LikelihoodProfiler.chi2_quantile","page":"API","text":"chi2_quantile(α, df=1)\n\nComputes α quantile for Chi2 distribution with df degrees of freedom\n\n\n\n\n\n","title":"LikelihoodProfiler.chi2_quantile"},{"category":"method","location":"api/#LikelihoodProfiler.endpoints-Tuple{LikelihoodProfiler.ProfileCurve}","page":"API","text":"endpoints(pc::ProfileCurve; xtransform=identity)\nendpoints(sol::ProfileLikelihoodSolution; xtransform=identity)\n\nReturn the estimated left and right confidence-interval endpoints at the profile's likelihood threshold. Calling endpoints on a ProfileLikelihoodSolution returns the endpoints for every profile.\n\nThe xtransform function is applied to each estimated endpoint before it is returned. It defaults to identity; for example, use xtransform=exp10 to report endpoints on the original parameter scale when profiling was performed in base-10 log space.\n\n\n\n\n\n","title":"LikelihoodProfiler.endpoints"},{"category":"function","location":"api/#LikelihoodProfiler.evaluate_FIM","page":"API","text":"evaluate_FIM(plprob::ProfileLikelihoodProblem, θ=plprob.optpars)\n\nEvaluates the Fisher Information Matrix (FIM) at the given parameter values θ (default: plprob.optpars).  This is the local curvature used by QuadraticApproxProfiler to build the quadratic approximation near the optimum.\n\n\n\n\n\n","title":"LikelihoodProfiler.evaluate_FIM"},{"category":"type","location":"api/#LikelihoodProfiler.AdaptiveInitialStep","page":"API","text":"AdaptiveInitialStep(; rel_step=0.01, abs_step=1e-3, min_step=1e-4, max_step=Inf)\n\nInitial profile step rule that adapts to the current profiled parameter value.\n\nThe proposed step is max(rel_step * abs(x), abs_step), where x is the current profiled parameter value. The result is finally clamped to [min_step, max_step].\n\nThis is useful when parameters have different numerical scales and a single fixed absolute step would be inefficient.\n\n\n\n\n\n","title":"LikelihoodProfiler.AdaptiveInitialStep"},{"category":"type","location":"api/#LikelihoodProfiler.AdaptiveStep","page":"API","text":"AdaptiveStep(; initial_step=AdaptiveInitialStep(),\n               predictor=LinearPredictor(),\n               controller=ObjectiveStepControl())\n\nProfiler stepper that adapts the profile step length based on the observed objective increase from trial points.\n\nThe first profile step uses initial_step. Later steps use predictor to choose the direction in parameter space and controller to keep the next objective increase in a useful range.\n\n\n\n\n\n","title":"LikelihoodProfiler.AdaptiveStep"},{"category":"type","location":"api/#LikelihoodProfiler.CICOProfiler","page":"API","text":"CICOProfiler\n\nConfidence Intervals by Constrained Optimization (CICO) method to find the intersections of the likelihood function with the threshold. See CICOBase docs for more details. Requires using CICOBase.\n\nFields\n\noptimizer::Symbol: The optimizer used for the optimization process. Defaults to NLopt :LN_NELDERMEAD.\nscan_tol::Float64: The tolerance for the endpoints scan. Defaults to 1e-3.\n\nExample\n\nprofiler = CICOProfiler(optimizer = :LN_NELDERMEAD, scan_tol = 1e-3)\n\n\n\n\n\n","title":"LikelihoodProfiler.CICOProfiler"},{"category":"type","location":"api/#LikelihoodProfiler.FixedStep","page":"API","text":"FixedStep{S}\n\nProfiler stepper that proposes each profile point from the configured initial_step rule without adapting to trial objective values.\n\nConstructors\n\nFixedStep(;initial_step=AdaptiveInitialStep())\n\nKeyword arguments\n\ninitial_step=AdaptiveInitialStep(): The step rule to use for each profile step. This can be a number (for a constant absolute step size), an AdaptiveInitialStep, or a callable ctx -> step for custom logic depending on the current profiler cache. If a number is provided, it is automatically wrapped as a function.\n\n\n\n\n\n","title":"LikelihoodProfiler.FixedStep"},{"category":"type","location":"api/#LikelihoodProfiler.FunctionTarget","page":"API","text":"FunctionTarget{F,B}\n\nProfile target representing profiling of functions of model parameters.\n\nFields\n\nfs::AbstractVector{<:OptimizationFunction}: Functions of the parameters being profiled.\nprofile_lower::AbstractVector{<:Real}: Lower bounds for the profile likelihood. \nprofile_upper::AbstractVector{<:Real}: Upper bounds for the profile likelihood. \nlabels::Union{Nothing,AbstractVector{<:Symbol}}: Optional labels for profiled functions (same length/order as fs).\n\nProfile bounds profile_lower and profile_upper should be vectors of finite numerical values. \n\nConstructors\n\nCreate a target with explicit lower and upper bounds for each function of parameters.\n\nFunctionTarget(; fs::AbstractVector{<:OptimizationFunction}, profile_lower::AbstractVector{<:Real}, profile_upper::AbstractVector{<:Real}, labels=nothing)\n\n\n\n\n\n","title":"LikelihoodProfiler.FunctionTarget"},{"category":"type","location":"api/#LikelihoodProfiler.IntegrationProfiler","page":"API","text":"IntegrationProfiler{opType, optsType, DEAlg, DEOpts}\n\nA profiler method that uses integration of differential equations system to profile the likelihood function.\n\nFields\n\nreoptimize::Bool: Indicates whether to re-optimization after each step of the integrator. Defaults to false.\noptimizer::opType: The optimizer used for the optimization process. Defaults to nothing.\noptimizer_opts::optsType: Options for the optimizer. Defaults to NamedTuple().\nintegrator::DEAlg: The differential equation algorithm used for integration.\nintegrator_opts::DEOpts: Options for the differential equation solver. Defaults to NamedTuple().\nmatrix_type::Symbol: The type of matrix to be used for the Hessian approximation. Possible options are: :hessian, :identity. Defaults to :hessian.\ngamma::Float64: Correction factor used in integration if full hessian is not computed (e.g. matrix_type = :identity). Defaults to 1.0.\n\nExample\n\nusing OrdinaryDiffEqTsit5\nprofiler = IntegrationProfiler(integrator = Tsit5(), integrator_opts = (dtmax=0.3,), matrix_type = :hessian)\n\n\n\n\n\n","title":"LikelihoodProfiler.IntegrationProfiler"},{"category":"type","location":"api/#LikelihoodProfiler.LinearPredictor","page":"API","text":"LinearPredictor()\n\nExtrapolates all parameters using linear extrapolation based on the last two successful profile points.\n\n\n\n\n\n","title":"LikelihoodProfiler.LinearPredictor"},{"category":"type","location":"api/#LikelihoodProfiler.ObjectiveStepControl","page":"API","text":"ObjectiveStepControl(; threshold_fraction=0.1,\n                       target_factor=1.5,\n                       lower_factor=0.25,\n                       min_obj_step=1e-2,\n                       max_obj_step=Inf,\n                       min_x_step=1e-4,\n                       max_x_step=Inf,\n                       step_factor=1.5,\n                       max_x_step_growth=5.0,\n                       maxiters=15)\n\nControls adaptive profile stepping by defining acceptable objective and profile-axis step ranges.\n\nFor finite likelihood thresholds, the target objective increase is based on threshold_fraction * threshold. For infinite thresholds, the target increase is based on target_factor * abs(obj_cur - obj_prev).\n\nThe previous optimized profile objective change is used to nudge the first trial step up or down. Trial points below the upper objective target are then treated as safe to grow until the upper target, profile bound, or iteration limit is reached. The next profile-axis step is additionally capped by max_x_step_growth * previous_step to avoid abrupt jumps in flat profile regions.\n\n\n\n\n\n","title":"LikelihoodProfiler.ObjectiveStepControl"},{"category":"type","location":"api/#LikelihoodProfiler.OptimizationProfiler","page":"API","text":"OptimizationProfiler{S, opType, optsType}\n\nA profiler method that uses stepwise re-optimization to profile the likelihood function.\n\nFields\n\nstepper::S: The algorithm used to compute the next profile point. Supported steppers include:\nFixedStep: Proposes steps from the configured step rule in the profiling direction (Default). \nAdaptiveStep: Adapts the step size based on trial objective increases.\noptimizer::opType: The optimizer used for the optimization process.\noptimizer_opts::optsType: Options for the optimizer. Defaults to NamedTuple().\n\nStepping Options\n\nThe stepper argument controls how the next profile point is chosen. For example:\n\nstepper = FixedStep(initial_step=0.1): Use a constant step size of 0.1.\nstepper = AdaptiveStep(predictor=LinearPredictor()): Use adaptive stepping with a secant-like linear predictor.\n\nSee the documentation for each stepper type (e.g., ?FixedStep, ?AdaptiveStep) for more details and customization options.\n\nExample\n\nusing OptimizationLBFGSB\nprofiler = OptimizationProfiler(; optimizer = LBFGSB(), optimizer_opts = (reltol=1e-4,))\n\n\n\n\n\n","title":"LikelihoodProfiler.OptimizationProfiler"},{"category":"type","location":"api/#LikelihoodProfiler.ParameterTarget","page":"API","text":"ParameterTarget{I,B}\n\nProfile target representing profiling of model parameters.\n\nFields\n\nidxs::AbstractVector{<:Integer}: Indices of the parameters being profiled.\nprofile_lower::AbstractVector{<:Real}: Lower bounds for the profile likelihood. \nprofile_upper::AbstractVector{<:Real}: Upper bounds for the profile likelihood. \nlabels::Union{Nothing,AbstractVector{<:Symbol}}: Optional labels for profiled quantities (same length/order as idxs).\n\nProfile bounds profile_lower and profile_upper should be vectors of finite numerical values. \n\nConstructors\n\nCreate a target with explicit lower and upper bounds for each index.\n\nParameterTarget(; idxs::AbstractVector{<:Integer}, profile_lower::AbstractVector{<:Real}, profile_upper::AbstractVector{<:Real}, labels=nothing)\n\n\n\n\n\n","title":"LikelihoodProfiler.ParameterTarget"},{"category":"type","location":"api/#LikelihoodProfiler.ProfileLikelihoodProblem","page":"API","text":"ProfileLikelihoodProblem{T,probType,P}\n\nDefines a profile likelihood problem.\n\nMathematical Specification of a Profile Likelihood Problem:\n\nA problem is specified by:\n\noptprob::OptimizationProblem — wraps your objective (e.g. negative log-likelihood)\noptpars::AbstractVector{<:Real} — parameter values to start profiling from (typically the optimum)\ntarget::AbstractProfileTarget — what to profile (parameters or functions)\n\nConstructors\n\nExplicit target interface (advanced)\n\nProfileLikelihoodProblem(optprob::OptimizationProblem, optpars::AbstractVector{<:Real}, target::AbstractProfileTarget; \n  conf_level::Float64 = 0.95, df::Int = 1, threshold::Union{Nothing,Real} = nothing)\n\ntarget: ParameterTarget (see ParameterTarget) or FunctionTarget (see FunctionTarget) defining what to profile and the profile bounds.\nconf_level: Confidence level for the profile likelihood. Defaults to 0.95.\ndf: Degrees of freedom for the profile likelihood. Defaults to 1.\nthreshold: Profile likelihood threshold. If not provided, computed from conf_level and df. Can be set to Inf if confidence interval endpoint estimation is not required.\n\nParameter profiling sugar\n\nProfileLikelihoodProblem(optprob::OptimizationProblem, optpars::AbstractVector{<:Real};\n  idxs = nothing, profile_lower = nothing, profile_upper = nothing, kwargs...)\n\nidxs: Indices of parameters to profile; Integer, vector of Integers, Symbol, or vector of Symbols; if nothing, profile all parameters. Symbolic indices are resolved against inferred labels of optpars (e.g. named ComponentArray).\nprofile_lower, profile_upper: Bounds for profiling. Accept scalars or vectors of finite numbers; if nothing, taken from optprob. If scalar bounds are provided, they will be expanded to match the number of parameters being profiled.\nkwargs...: passed to the explicit target constructor.\n\nFunction profiling sugar\n\nProfileLikelihoodProblem(optprob::OptimizationProblem, optpars::AbstractVector{<:Real}, fs;\n  profile_lower = nothing, profile_upper = nothing, kwargs...)\n\nfs: OptimizationFunction, vector of OptimizationFunction, or named container of OptimizationFunctions. Named containers propagate keys to profile labels.\n\nprofile_lower, profile_upper: Bounds for profiling. Accept scalars or vectors of finite numbers. If scalar bounds are provided, they will be expanded to match the number of functions being profiled.\nkwargs...: passed to the explicit target constructor.\n\n\n\n\n\n","title":"LikelihoodProfiler.ProfileLikelihoodProblem"},{"category":"type","location":"api/#LikelihoodProfiler.ProfileLikelihoodSolution","page":"API","text":"ProfileLikelihoodSolution{probType,P}\n\nContains the results of a profile likelihood analysis.\n\nFields\n\nprob::probType: The profile likelihood problem ProfileLikelihoodProblem.\nprofiles::P: The computed profile curves.\nelapsed_time::Float64: The time elapsed during the computation.\n\nSelectors\n\nA number of selectors are available to extract information from the sol::ProfileLikelihoodSolution object. These can be applied to each computed profile sol[i]:\n\nendpoints(sol[i]): Returns the confidence interval (CI) endpoints, marking the intersection of the profile with the threshold.\nretcodes(sol[i]): Returns the retcodes of the CI endpoints estimation.\nstats(sol[i]): Returns the statistics of the profile computation.\nprofile_labels(sol): Returns labels of profiled quantities, if available.\n\nIndexing\n\nsol[i::Int]: Access profile by position.\nsol[s::Symbol]: Access profile by symbolic label (requires labels).\n\n\n\n\n\n","title":"LikelihoodProfiler.ProfileLikelihoodSolution"},{"category":"type","location":"api/#LikelihoodProfiler.QuadraticApproxProfiler","page":"API","text":"QuadraticApproxProfiler\n\nQuadratic-approximation confidence intervals (Wald approximation) based on local curvature at the optimum. The curvature is approximated by the Fisher Information Matrix/Hessian, so the resulting confidence intervals reflect the local quadratic approximation of the likelihood around optpars. By default this method reuses Hessian logic from OptimizationProblem (user-supplied Hessian or AD backend). The confidence interval is computed as θ̂ ± z * sqrt(Σ[idx, idx]), where          - θ̂ is the optpars[idx],          - z is the quantile of the chi-squared distribution corresponding to the conf_level and df parameters of the ProfileLikelihoodProblem,         - Σ is the covariance matrix obtained by inverting the FIM.\n\ncov_factor controls Hessian/objective scaling conventions. Common choices are:\n\n1.0 when Hessian is for -logL.\n2.0 when Hessian is for -2logL and you want covariance on the -logL scale.\n\nAny strictly positive value is allowed (not only 1 or 2), which can be useful for calibrated or robust variance scaling.\n\nFields\n\ninversion::Symbol: Matrix inversion strategy (:cholesky, :pinv).\nclamp_to_bounds::Bool: Clip estimated interval endpoints to profile bounds.\ncov_factor::Real: Multiplicative factor applied to inv(H) to obtain covariance (Σ = cov_factor * inv(H)).\nresolution::Int: Number of points per branch (left/right) used to sample the quadratic approximation.\n\n\n\n\n\n","title":"LikelihoodProfiler.QuadraticApproxProfiler"},{"category":"type","location":"api/#LikelihoodProfiler.SingleAxisPredictor","page":"API","text":"SingleAxisPredictor()\n\nExtrapolates only the profiled parameter using linear extrapolation based on the last two successful profile points.\n\n\n\n\n\n","title":"LikelihoodProfiler.SingleAxisPredictor"},{"category":"section","location":"parallel_modes/#Parallel-execution","page":"Parallel execution","text":"LikelihoodProfiler.jl supports parallel computation to accelerate profile likelihood calculations. All implemented methods support independent computations across parameters and profile branches (\"left\" and \"right\"). Depending on your system configuration and workload, you can leverage either multithreading or distributed processing (workers) for parallel execution.","title":"Parallel execution"},{"category":"section","location":"parallel_modes/#Available-parallel-modes","page":"Parallel execution","text":"Parallelization is controlled via the parallel_type keyword argument in the solve function.\n\nsolve(plprob, method; parallel_type = :none)\n\nSupported values:\n\n:none (default): Run all profiling computations sequentially.\n:threads: Use Julia's multithreading (i.e. Base.Threads.@threads) across available CPU threads. To enable multithreading, set the JULIA_NUM_THREADS environment variable.\n:distributed: Use Distributed.jl to run tasks across multiple Julia processes (e.g., started via addprocs() or julia -p N).","title":"Available parallel modes"},{"category":"section","location":"parallel_modes/#Distributed-workers-example","page":"Parallel execution","text":"The following example illustrates how to compute profile likelihoods using distributed parallelism across Julia processes in a multi-core (or multi-node) environment.\n\nusing Distributed\n\naddprocs(2)\n\n@everywhere using LikelihoodProfiler, OptimizationLBFGSB\n\n@everywhere rosenbrock(x,p) = (1.0 - x[1])^2 + 100.0*(x[2] - x[1]^2)^2\n\nx0 = [1., 1.]\noptf = OptimizationFunction(rosenbrock, AutoForwardDiff())\noptprob = OptimizationProblem(optf, x0)\n\nplprob = ProfileLikelihoodProblem(optprob, x0; profile_lower=-5.0, profile_upper=5.0, threshold = 1.0)\nmeth = OptimizationProfiler(optimizer = LBFGSB(), stepper = AdaptiveStep())\n\nsol = solve(plprob, meth; parallel_type=:distributed)","title":"Distributed workers example"},{"category":"section","location":"solution_interface/#solution_interface","page":"Solution interface","text":"ProfileLikelihoodSolution type is designed to contain the results of a profile likelihood analysis.","title":"Solution Interface"},{"category":"section","location":"solution_interface/#Retcodes","page":"Solution interface","text":"sol::ProfileLikelihoodSolution outputs the following retcodes, which are accessible with retcodes(sol[i]) function:\n\n:Identifiable - the profile has intersection with the predefined threshold.\n:NonIdentifiable - the profile doesn't intersect the predefined threshold.\n:MaxIters - maximum number of iterations reached while computing the profile. See maxiters argument to the solve.\n:Failure - the solver (optimizer or integrator) reported failure status, profiling was interrupted.","title":"Retcodes"},{"category":"section","location":"solution_interface/#Endpoints-(confidence-interval-estimates)","page":"Solution interface","text":"endpoints(sol[i]) returns the estimated crossing points of the profile with the chosen likelihood threshold for the i-th profile.","title":"Endpoints (confidence-interval estimates)"},{"category":"section","location":"solution_interface/#Visualization-and-tabular-representation","page":"Solution interface","text":"The recipes are defined to visualize profiles saved in sol::ProfileLikelihoodSolution with Plots.jl package: plot(sol), plot(sol[i]). The following keyword arguments can be used in plot function:\n\nsteps::Bool - whether to scatter steps performed by the profiler. Defaults to true.\nthreshold::Bool - whether to plot threshold defined in ProfileLikelihoodProblem. Defaults to isfinite(threshold)\nxtransform::Function - transform applied to x-values before plotting (e.g. exp10 to show log10-parameter profiles in linear space). Defaults to identity.\n\nAlso each profile contained in the sol::ProfileLikelihoodSolution can be represented as a DataFrame with DataFrame(sol[i]). If profile labels are present, symbolic indexing is available: sol[:label]. Use profile_labels(sol) to inspect available labels. When parameter labels are available, DataFrame(sol[i]) uses them for all parameter columns. For function targets, the additional column is named by the profiled function label (or :objective if no label exists).","title":"Visualization and tabular representation"},{"category":"type","location":"solution_interface/#LikelihoodProfiler.ProfileLikelihoodSolution-solution_interface","page":"Solution interface","text":"ProfileLikelihoodSolution{probType,P}\n\nContains the results of a profile likelihood analysis.\n\nFields\n\nprob::probType: The profile likelihood problem ProfileLikelihoodProblem.\nprofiles::P: The computed profile curves.\nelapsed_time::Float64: The time elapsed during the computation.\n\nSelectors\n\nA number of selectors are available to extract information from the sol::ProfileLikelihoodSolution object. These can be applied to each computed profile sol[i]:\n\nendpoints(sol[i]): Returns the confidence interval (CI) endpoints, marking the intersection of the profile with the threshold.\nretcodes(sol[i]): Returns the retcodes of the CI endpoints estimation.\nstats(sol[i]): Returns the statistics of the profile computation.\nprofile_labels(sol): Returns labels of profiled quantities, if available.\n\nIndexing\n\nsol[i::Int]: Access profile by position.\nsol[s::Symbol]: Access profile by symbolic label (requires labels).\n\n\n\n\n\n","title":"LikelihoodProfiler.ProfileLikelihoodSolution"},{"category":"section","location":"case_studies/gaussian/#Mean-of-a-Gaussian-Distribution","page":"Mean of a Gaussian Distribution","text":"This tutorial demonstrates the basic workflow of LikelihoodProfiler.jl using one of the simplest statistical models: estimating the mean of a Gaussian distribution with known variance.","title":"Mean of a Gaussian Distribution"},{"category":"section","location":"case_studies/gaussian/#Model-and-Data","page":"Mean of a Gaussian Distribution","text":"We assume we observe i.i.d. data:\n\nX_1 ldots X_n sim mathcalN(mu 1)\n\nand want to construct a confidence interval for the mean parameter μ. \n\nusing LikelihoodProfiler, OptimizationLBFGSB, Distributions, Random\nusing Plots\n\nRandom.seed!(73612768)\n\nn_obs = 10\ndata = rand(Normal(0, 1), n_obs)","title":"Model and Data"},{"category":"section","location":"case_studies/gaussian/#Objective-Function-(Negative-Log-Likelihood)","page":"Mean of a Gaussian Distribution","text":"With known variance sigma^2 = 1, the log-likelihood for μ is:\n\nell(mu) = sum_i=1^n log mathcalN(x_i mid mu 1)\n\nWe minimize negative log-likelihood:\n\nobj(x, p) = -sum(logpdf.(Normal(x[1], 1.0), data))","title":"Objective Function (Negative Log-Likelihood)"},{"category":"section","location":"case_studies/gaussian/#Maximum-Likelihood-Estimate","page":"Mean of a Gaussian Distribution","text":"x0 = [1.0]   # initial guess\n\noptf = OptimizationFunction(obj, AutoForwardDiff())\noptprob = OptimizationProblem(optf, x0)\nsol = solve(optprob, LBFGSB())\n\noptpars = sol.u   # MLE of μ\n\nFor a Gaussian with known variance, this should be very close to the sample mean.","title":"Maximum Likelihood Estimate"},{"category":"section","location":"case_studies/gaussian/#Profile-Likelihood-for-the-Mean","page":"Mean of a Gaussian Distribution","text":"We now build the ProfileLikelihoodProblem, define profiling method and solve the problem. In this model there is one parameter, so the profile is one-dimensional.\n\nplprob = ProfileLikelihoodProblem( optprob, optpars; \n    profile_lower = -3.0, profile_upper = 3.0)\n\nmethod = OptimizationProfiler(optimizer = LBFGSB(),\n    stepper = FixedStep(; initial_step = 0.01))\n\nsol = solve(plprob, method)\nplot(sol)","title":"Profile Likelihood for the Mean"},{"category":"section","location":"case_studies/gaussian/#Interpreting-the-plot","page":"Mean of a Gaussian Distribution","text":"The curve shows how the negative log-likelihood increases as μ moves away from its MLE.\nThe horizontal line marks the confidence threshold for the chosen level (default 95%).\nThe intersection points give the profile likelihood confidence interval.","title":"Interpreting the plot"},{"category":"section","location":"case_studies/gaussian/#Extracting-Confidence-Interval-Endpoints","page":"Mean of a Gaussian Distribution","text":"ProfileLikelihoodSolution stores the computed profile curves together with confidence interval endpoints and identification retcodes, which indicate whether each parameter is practically identifiable. These values can be accessed directly:\n\nretcodes(sol)\nendpoints(sol)","title":"Extracting Confidence Interval Endpoints"},{"category":"section","location":"case_studies/taxol/#Taxol-model","page":"Taxol model","text":"As an example of practical identifiability analysis, we use the Cancer Taxol Treatment Model. This model was proposed as a case-study for identifiability by Marisa C.Eisenberg, Harsh V.Jain. We have translated the model from cancer-chemo-identifiability repo into the Julia language.\n\nThe model has three states:\n\nP(t) - the number of cells in G2/M phase of the cell cycle\nAp(t) - the number of cells in G1/S phase of the cell cycle\nAr(t) - the number of cells arrested in G2/M due to drug action\n\nAnd five unknown parameters:\n\na0 - the maximum rate of arrest of cells in G2/M\nka - the sensitivity of proliferating cells to taxol\nr0 - the rate of arrested cell recovery to the proliferating pool\nd0 - the maximum rate of arrested cell death\nkd - the arrested cell death rate half-saturation constant\n\nWe start with loading the necessary packages:\n\n#=\nusing Pkg;\nPkg.add([\n  \"LikelihoodProfiler\", \n  \"OptimizationLBFGSB\", \n  \"OrdinaryDiffEqTsit5\", \n  \"Distributions\", \n  \"ComponentArrays\", \n  \"Plots\"\n])\n=#\nusing LikelihoodProfiler\nusing OptimizationLBFGSB\nusing OrdinaryDiffEqTsit5\nusing Distributions\nusing ComponentArrays\nusing Plots\n\nWe define the constants (fixed parameters):\n\nVt - total available volume / carrying capacity\nV0 - half-saturation free volume; crowding threshold\nlam - the tumor cell division rate\naRP - transition rate from G1/S to G2/M\narstexp - Hill function coefficients for drug effects on the cells\nadthexp - Hill function coefficients for drug effects on the cells\ntheta - Hill coefficient for crowding\n\n  const Vt      = 10.515*100\n  const V0      = 1.3907*Vt\n  const lam     = 9.5722\n  const aRP     = 20\n  const arstexp = 3\n  const adthexp = 4\n  const theta   = 10;\n\nThe model is defined as a system of ordinary differential equations (ODEs) with the following structure:\n\n# https://github.com/marisae/cancer-chemo-identifiability/blob/master/Profile%20Likelihood/testa0_de.m\nfunction taxol_ode(du, u, p, t)\n  a0, ka, r0, d0, kd = p.params\n  drug = p.drug\n\n  Ncel = u[:P] + u[:Ap] + u[:Ar]\n  Lfac = ((Vt-Ncel)^theta)/((V0^theta) + ((Vt-Ncel)^theta))\n\n  arst = a0*(drug^arstexp)/(ka^arstexp + (drug^arstexp))\n  adth = d0*(drug^adthexp)/(kd^adthexp + (drug^adthexp))\n  arcv = r0\n\n  du[:P] = -lam*u[:P] + aRP*u[:Ap]*Lfac - arst*u[:P] + arcv*u[:Ar]\n  du[:Ap] = 2*lam*u[:P] - aRP*u[:Ap]*Lfac\n  du[:Ar] = arst*u[:P] - adth*u[:Ar] - arcv*u[:Ar]\n  \n  return nothing\nend;\n\nWe define the initial conditions for the ODE system, and optimal parameters for the model, which were obtained from Marisa's Matlab code. Finally, we define the time span for the simulation and create an ODEProblem instance.\n\nu0 = ComponentArray(\n  P = 7.2700, \n  Ap = 2.5490, \n  Ar = 0.0\n)\n\nfunction taxol_params(x, d)\n  return ComponentArray(params = x, drug = d)\nend\n\ntaxol_params0 = ComponentArray(\n  a0 = 8.3170,\n  ka = 8.0959,\n  r0 = 0.0582,\n  d0 = 1.3307,\n  kd = 119.1363,\n  )\n\np0 = taxol_params(taxol_params0, 5.0)\n\ntspan = (0.,15.)\node_prob = ODEProblem(taxol_ode, u0, tspan, p0)\n\nNext, we define the experimental data for the model. The data consists of measurements of the number of cells at different time points and for doses of Taxol (5, 10, 40, 100) ng/ml.\n\n# https://github.com/marisae/cancer-chemo-identifiability/blob/master/Profile%20Likelihood/testa0_fit.m\n\ntimes = [0., 3., 6., 9., 12., 15.]   # days\ndose = [5., 10., 40., 100.];    # dose in ng/ml\n\n# Control data\nCell = [0.009, 0.050, 0.120, 0.189, 0.230, 0.260]*1091.0   # thousands of cells\nCerr = [0.006, 0.012, 0.010, 0.011, 0.011, 0.011]*1091.0   # thousands of cells\n\n# 0.005 ug/ml Taxol\nCell005 = [0.009, 0.047, 0.089, 0.149, 0.198, 0.219]*1091.0   # thousands of cells\nCerr005 = [0.006, 0.013, 0.010, 0.011, 0.013, 0.010]*1091.0   # thousands of cells\n\n# 0.010 ug/ml Taxol\nCell010 = [0.009, 0.043, 0.077, 0.093, 0.109, 0.128]*1091.0   # thousands of cells\nCerr010 = [0.006, 0.012, 0.013, 0.012, 0.014, 0.012]*1091.0   # thousands of cells\n\n# 0.040 ug/ml Taxol\nCell040 = [0.009, 0.025, 0.047, 0.054, 0.076, 0.085]*1091.0   # thousands of cells\nCerr040 = [0.005, 0.010, 0.010, 0.011, 0.010, 0.010]*1091.0   # thousands of cells\n\n# 0.100 ug/ml Taxol\nCell100 = [0.009, 0.025, 0.026, 0.028, 0.029, 0.031]*1091.0   # thousands of cells\nCerr100 = [0.006, 0.010, 0.009, 0.008, 0.011, 0.011]*1091.0   # thousands of cells\n\nC005 = mean(Cell005)\nC010 = mean(Cell010)\nC040 = mean(Cell040)\nC100 = mean(Cell100)\n\ndata = [Cell005/C005, Cell010/C010, Cell040/C040, Cell100/C100]\ndatamean = [C005, C010, C040, C100];\n\nWe define the solver setup and the objective function for the optimization problem.\n\nsolver_opts = (\n    alg = Tsit5(),\n    reltol = 1e-6,\n    abstol = 1e-8,\n)\n\nfunction taxol_obj(x, hp)\n\n  loss = 0.\n  for (i,d) in enumerate(dose)\n    prob = remake(ode_prob; p = taxol_params(exp10.(x), d))\n    sol = solve(\n      prob, \n      solver_opts.alg, \n      reltol=solver_opts.reltol,\n      abstol=solver_opts.abstol,\n      saveat=times)\n    if !SciMLBase.successful_retcode(sol)\n     return Inf\n    end\n      \n    for time_idx in eachindex(data[i])\n      sim = (sol[1, time_idx] + sol[2, time_idx] + sol[3, time_idx]) / datamean[i]\n      loss += abs2(sim - data[i][time_idx])\n    end\n  end\n  return loss\nend\n\nConfidence-level for the profile likelihood threshold is also chosen according to the original paper. The threshold line represents the value of the objective function (or negative log-likelihood) that corresponds to the chosen confidence level. Where the profile curve intersects this threshold gives the lower and upper CI bounds.\n\n# https://github.com/marisae/cancer-chemo-identifiability/blob/master/Profile%20Likelihood/testa0_fit.m#L40-L41\n\ntaxol_relative_errors = vcat(\n    Cerr005 / C005,\n    Cerr010 / C010,\n    Cerr040 / C040,\n    Cerr100 / C100,\n)\nsigmasq = mean(taxol_relative_errors)^2\n\nWe optimize the logarithms of the parameters rather than their values on the original scale. Optimization in log space is often recommended because it puts parameters with different orders of magnitude on a more common scale, which can improve the numerical behavior of the optimizer. The objective function therefore converts x back to the original parameter scale with exp10.(x) before solving the ODE.\n\nWe define the initial parameter values and bounds in log space and wrap the objective function into an OptimizationProblem instance. The fitted parameters use a flat, named ComponentArray, separate from the nested ODE parameters that also contain the drug dose. This allows ProfileLikelihoodProblem to infer the parameter labels automatically.\n\nopt_params0 = log10.(taxol_params0)\nlb = log10.(ComponentArray(\n  a0 = 2.0,\n  ka = 2.0,\n  r0 = 0.01,\n  d0 = 0.05,\n  kd = 30.0,\n))\nub = log10.(ComponentArray(\n  a0 = 30.0,\n  ka = 30.0,\n  r0 = 0.6,\n  d0 = 10.0,\n  kd = 210.0,\n))\n\noptf = OptimizationFunction(taxol_obj, AutoForwardDiff())\noptprob = OptimizationProblem(optf, opt_params0; lb, ub)\n\nTo define the profile likelihood problem, we use the OptimizationProblem and optimal (best fit) values of the parameters. Because the optimization variables are logarithms, the profile likelihood is also computed in log space. We can also specify the threshold for the profile likelihood, define the profile bounds and other options for the ProfileLikelihoodProblem.\n\nNext we choose the profile likelihood algorithm and solve the problem. Here we use an OptimizationProfiler which steps through the parameter of interest and re-optimizes the nuisance parameters at each step. This stepping is done adaptively. The direction of the step is chosen on the basis of previous successful steps and the step size is adjusted based on linesearch.\n\nWe can also set reoptimize_init=true to re-optimize the initial starting point to reassure we start profiling from the optimum.\n\nplprob = ProfileLikelihoodProblem(optprob, opt_params0; threshold = sigmasq*chi2_quantile(0.95, 5))\n\nalg_opt = OptimizationProfiler(optimizer = LBFGSB(),  stepper = AdaptiveStep())\n\nsol = solve(plprob, alg_opt; reoptimize_init=true)\n\nProfileLikelihoodSolution stores the computed profile curves together with confidence interval endpoints and identification retcodes, which indicate whether each parameter is practically identifiable. These values can be accessed directly:\n\nretcodes(sol)\nendpoints(sol; xtransform=exp10)\n\nFinally, we plot the resulting profiles, using xtransform=exp10 to display the parameter axes on their original scale. Each point on the curve corresponds to a profiler step, i.e., a constrained optimization performed at a fixed value of the profiled parameter. The horizontal line indicates the likelihood threshold for the chosen confidence level; its intersections with the curve define the confidence interval bounds. Steeper profiles indicate better identifiability, whereas flat curves or curves that never intersect the threshold indicate parameters that are not practically identifiable.\n\nplot(sol, xtransform=exp10)","title":"Taxol model"},{"category":"section","location":"#LikelihoodProfiler.jl:-Unified-Interface-to-Profile-Likelihood-Methods","page":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods","text":"LikelihoodProfiler.jl is a Julia package for practical identifiability analysis and confidence intervals estimation using the profile likelihood approach. The package provides a unified interface for various profile likelihood methods, including optimization-based OptimizationProfiler and integration-based profiles IntegrationProfiler, CI endpoints search CICOProfiler, and more.","title":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods"},{"category":"section","location":"#Who-is-this-package-for?","page":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods","text":"LikelihoodProfiler.jl is intended for researchers and practitioners working with maximum-likelihood estimation (MLE) problems in any scientific or engineering domain. The package does not assume a specific model type and can be applied to statistical models, mechanistic models, models defined through simulations, optimization problems, or arbitrary likelihood functions. Applicable to a broad class of MLE problems, profile likelihood is especially valuable for complex nonlinear models — such as high-dimensional mechanistic models — where standard asymptotic confidence intervals can be unreliable.\n\nTypical application areas include (but are not limited to):\n\nSystems biology & Quantitative Systems Pharmacology,\nEngineering and control,\nScientific Machine Learning and any field requiring identifiability and uncertainty analysis of MLE parameters.","title":"Who is this package for?"},{"category":"section","location":"#What-problems-does-this-package-solve?","page":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods","text":"Profile likelihood methods provide insight into practical identifiability - how precisely model parameters (or predictions derived from them) are determined by the available data. LikelihoodProfiler.jl offers a unified interface for:\n\nParameter profile likelihoods. Profile the likelihood function to explore how well parameters are constrained by the data.\nConfidence intervals for parameters. Estimate confidence intervals for parameter values based on likelihood threshold to quantify the level of certainty in the parameter estimates.\nFunctional profile likelihoods.  Profile arbitrary functions of the parameters (e.g., predictions, reparameterizations, etc).\n\nThese capabilities apply to any setting where an MLE objective (e.g., negative log-likelihood) can be evaluated.","title":"What problems does this package solve?"},{"category":"section","location":"#Installation","page":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods","text":"In Julia terminal run the following command:\n\nimport Pkg; Pkg.add(\"LikelihoodProfiler\")","title":"Installation"},{"category":"section","location":"#Related-packages","page":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods","text":"Other implementations of the profile likelihood approach in Julia include:\n\nProfileLikelihood.jl implements fixed-step optimization-based profiles and supports bivariate profile likelihoods.\nInformationalGeometry.jl implements various methods to study likelihood functions (including profile likelihood) using the tools of differential geometry.\n\nThere are also well-known profile likelihood implementations in other languages, namely: Data2Dynamics, dMod, pyPESTO, sbioparametersci","title":"Related packages"},{"category":"section","location":"#Citation","page":"LikelihoodProfiler.jl: Unified Interface to Profile Likelihood Methods","text":"Borisov I., Metelkin E. An Algorithm for Practical Identifiability Analysis and Confidence Intervals Evaluation Based on Constrained Optimization. 2018. October. ICSB2018. https://doi.org/10.13140/RG.2.2.18935.06563","title":"Citation"},{"category":"section","location":"problem_interface/#Defining-a-ProfileLikelihoodProblem","page":"Problem interface","text":"ProfileLikelihoodProblem type is designed to contain the necessary information to define a profile likelihood problem.","title":"Defining a ProfileLikelihoodProblem"},{"category":"section","location":"problem_interface/#Targets-(what-you-profile)","page":"Problem interface","text":"A profile target specifies the quantity whose profile you want: either one or more parameters by index, or one or more functions of the parameters. Targets carry their own profile bounds (profile_lower, profile_upper) which delimit where each profile is traced.","title":"Targets (what you profile)"},{"category":"section","location":"problem_interface/#ParameterTarget","page":"Problem interface","text":"ParameterTarget profiles model parameters by index and can optionally carry labels (symbols aligned with profiled indices). While you can build ParameterTarget directly, most users prefer the convenience constructor on ProfileLikelihoodProblem, which accepts idxs and bounds and constructs the target for you. See ProfileLikelihoodProblem interface.","title":"ParameterTarget"},{"category":"section","location":"problem_interface/#Explicit-construction-example","page":"Problem interface","text":"using LikelihoodProfiler\n\nf = OptimizationFunction((θ,p)->sum(abs2, θ))\noptprob = OptimizationProblem(f, [1.0, 2.0, 3.0])\n\npt = ParameterTarget(; idxs=[1,3],\n                     profile_lower=[-5.0, -1.0],\n                     profile_upper=[ 2.0,  4.0],\n                     labels=[:a, :c])\n\nplprob = ProfileLikelihoodProblem(optprob, [0.0, 0.0, 0.0], pt;\n                                  conf_level=0.95)  # threshold derived from χ²","title":"Explicit construction example"},{"category":"section","location":"problem_interface/#FunctionTarget","page":"Problem interface","text":"FunctionTarget profiles functions of the parameters and can optionally carry labels for each function. While you can build FunctionTarget directly, most users prefer the convenience constructor on ProfileLikelihoodProblem, which accepts functions and bounds and constructs the target for you. See ProfileLikelihoodProblem interface.","title":"FunctionTarget"},{"category":"section","location":"problem_interface/#Explicit-construction-example-2","page":"Problem interface","text":"using LikelihoodProfiler\n\nf = OptimizationFunction((θ,p)->sum(abs2, θ))\noptprob = OptimizationProblem(f, [1.0, 2.0, 3.0])\n\ng1 = OptimizationFunction((θ,p)->θ[1] + θ[2])\ng2 = OptimizationFunction((θ,p)->θ[2] - θ[3])\n\nft = FunctionTarget(; fs=[g1,g2],\n                    profile_lower=[-2.0, -1.0],\n                    profile_upper=[ 2.0,  1.0],\n                    labels=[:sum12, :diff23])\n\nplprob = ProfileLikelihoodProblem(optprob, [0.0, 0.0, 0.0], ft)","title":"Explicit construction example"},{"category":"section","location":"problem_interface/#Problem-“sugar”-constructors","page":"Problem interface","text":"For ergonomics, ProfileLikelihoodProblem provides keyword constructors that build the target for you.\n\nProfileLikelihoodProblem(::OptimizationProblem, ::AbstractVector{<:Real}; idxs, profile_lower, profile_upper)\nProfileLikelihoodProblem(::OptimizationProblem, ::AbstractVector{<:Real}, fs; profile_lower, profile_upper)\n\nidxs may contain integers (scalar/vector) or symbols (scalar/vector).\nFor symbolic parameter indexing (idxs=[:a,:b]), labels are inferred from named parameter containers (e.g. ComponentArray) when available.\nFor function targets, labels are inferred from named function containers (e.g. NamedTuple) when available.\n\nprofile_labels(plprob) returns active labels associated with profiled quantities.","title":"Problem “sugar” constructors"},{"category":"type","location":"problem_interface/#LikelihoodProfiler.ProfileLikelihoodProblem-problem_interface","page":"Problem interface","text":"ProfileLikelihoodProblem{T,probType,P}\n\nDefines a profile likelihood problem.\n\nMathematical Specification of a Profile Likelihood Problem:\n\nA problem is specified by:\n\noptprob::OptimizationProblem — wraps your objective (e.g. negative log-likelihood)\noptpars::AbstractVector{<:Real} — parameter values to start profiling from (typically the optimum)\ntarget::AbstractProfileTarget — what to profile (parameters or functions)\n\nConstructors\n\nExplicit target interface (advanced)\n\nProfileLikelihoodProblem(optprob::OptimizationProblem, optpars::AbstractVector{<:Real}, target::AbstractProfileTarget; \n  conf_level::Float64 = 0.95, df::Int = 1, threshold::Union{Nothing,Real} = nothing)\n\ntarget: ParameterTarget (see ParameterTarget) or FunctionTarget (see FunctionTarget) defining what to profile and the profile bounds.\nconf_level: Confidence level for the profile likelihood. Defaults to 0.95.\ndf: Degrees of freedom for the profile likelihood. Defaults to 1.\nthreshold: Profile likelihood threshold. If not provided, computed from conf_level and df. Can be set to Inf if confidence interval endpoint estimation is not required.\n\nParameter profiling sugar\n\nProfileLikelihoodProblem(optprob::OptimizationProblem, optpars::AbstractVector{<:Real};\n  idxs = nothing, profile_lower = nothing, profile_upper = nothing, kwargs...)\n\nidxs: Indices of parameters to profile; Integer, vector of Integers, Symbol, or vector of Symbols; if nothing, profile all parameters. Symbolic indices are resolved against inferred labels of optpars (e.g. named ComponentArray).\nprofile_lower, profile_upper: Bounds for profiling. Accept scalars or vectors of finite numbers; if nothing, taken from optprob. If scalar bounds are provided, they will be expanded to match the number of parameters being profiled.\nkwargs...: passed to the explicit target constructor.\n\nFunction profiling sugar\n\nProfileLikelihoodProblem(optprob::OptimizationProblem, optpars::AbstractVector{<:Real}, fs;\n  profile_lower = nothing, profile_upper = nothing, kwargs...)\n\nfs: OptimizationFunction, vector of OptimizationFunction, or named container of OptimizationFunctions. Named containers propagate keys to profile labels.\n\nprofile_lower, profile_upper: Bounds for profiling. Accept scalars or vectors of finite numbers. If scalar bounds are provided, they will be expanded to match the number of functions being profiled.\nkwargs...: passed to the explicit target constructor.\n\n\n\n\n\n","title":"LikelihoodProfiler.ProfileLikelihoodProblem"},{"category":"type","location":"problem_interface/#LikelihoodProfiler.ParameterTarget-problem_interface","page":"Problem interface","text":"ParameterTarget{I,B}\n\nProfile target representing profiling of model parameters.\n\nFields\n\nidxs::AbstractVector{<:Integer}: Indices of the parameters being profiled.\nprofile_lower::AbstractVector{<:Real}: Lower bounds for the profile likelihood. \nprofile_upper::AbstractVector{<:Real}: Upper bounds for the profile likelihood. \nlabels::Union{Nothing,AbstractVector{<:Symbol}}: Optional labels for profiled quantities (same length/order as idxs).\n\nProfile bounds profile_lower and profile_upper should be vectors of finite numerical values. \n\nConstructors\n\nCreate a target with explicit lower and upper bounds for each index.\n\nParameterTarget(; idxs::AbstractVector{<:Integer}, profile_lower::AbstractVector{<:Real}, profile_upper::AbstractVector{<:Real}, labels=nothing)\n\n\n\n\n\n","title":"LikelihoodProfiler.ParameterTarget"},{"category":"type","location":"problem_interface/#LikelihoodProfiler.FunctionTarget-problem_interface","page":"Problem interface","text":"FunctionTarget{F,B}\n\nProfile target representing profiling of functions of model parameters.\n\nFields\n\nfs::AbstractVector{<:OptimizationFunction}: Functions of the parameters being profiled.\nprofile_lower::AbstractVector{<:Real}: Lower bounds for the profile likelihood. \nprofile_upper::AbstractVector{<:Real}: Upper bounds for the profile likelihood. \nlabels::Union{Nothing,AbstractVector{<:Symbol}}: Optional labels for profiled functions (same length/order as fs).\n\nProfile bounds profile_lower and profile_upper should be vectors of finite numerical values. \n\nConstructors\n\nCreate a target with explicit lower and upper bounds for each function of parameters.\n\nFunctionTarget(; fs::AbstractVector{<:OptimizationFunction}, profile_lower::AbstractVector{<:Real}, profile_upper::AbstractVector{<:Real}, labels=nothing)\n\n\n\n\n\n","title":"LikelihoodProfiler.FunctionTarget"},{"category":"section","location":"rosenbrock/#Getting-Started-with-LikelihoodProfiler","page":"Getting Started","text":"LikelihoodProfiler.jl provides tools for computing profile likelihoods, confidence intervals, and prediction profiles for maximum-likelihood estimation (MLE) problems.   To define a ProfileLikelihoodProblem, you need two ingredients:\n\nan objective function (typically a negative log-likelihood or a least-squares loss), and  \nthe optimal parameter values, i.e., the parameter vector that minimizes the objective.\n\nProfile likelihood analysis requires evaluating the objective function under parameter perturbations. To define the objective function and its associated optimization problem LikelihoodProfiler.jl builds directly on the Optimization.jl interface. Internally, every ProfileLikelihoodProblem wraps an OptimizationProblem, so anything you can optimize with Optimization.jl can be profiled here.\n\nBelow we illustrate the basic workflow using a simple artificial objective function: the Rosenbrock function.   More realistic applications (ODE models, statistical models) are shown in the Tutorials section.\n\nFirst we define the OptimizationProblem and solve it with the preferred optimizer to obtain the optimal values of the parameters. ","title":"Getting Started with LikelihoodProfiler"},{"category":"section","location":"rosenbrock/#Define-and-solve-the-optimization-problem","page":"Getting Started","text":"We begin by defining the objective function and solving the optimization problem to obtain the optimal values of the parameters:\n\nusing LikelihoodProfiler, OptimizationLBFGSB, OrdinaryDiffEqTsit5, CICOBase\nusing Plots\n\n# objective function\nrosenbrock(x,p) = (1.0 - x[1])^2 + 100.0*(x[2] - x[1]^2)^2\n\n# initial values\nx0 = zeros(2)\n\n# solving optimization problem\noptf = OptimizationFunction(rosenbrock, AutoForwardDiff())\noptprob = OptimizationProblem(optf, x0)\nsol = solve(optprob, LBFGSB())","title":"Define and solve the optimization problem"},{"category":"section","location":"rosenbrock/#Profile-Likelihood-Problem-Interface","page":"Getting Started","text":"To construct a ProfileLikelihoodProblem, we provide the OptimizationProblem together with the optimal parameter values obtained from solving it. We can also set the profiling domain with the profile_lower, profile_upper arguments, indicies of parameters to profile with idxs and confidence settings (conf_level, df) used to compute the profile threshold for confidence intervals. Please consult ?ProfileLikelihoodProblem on the details of the interface.\n\n# optimal values of the parameters\noptpars = sol.u\n\n# profile likelihood problem\nplprob = ProfileLikelihoodProblem(optprob, optpars; profile_lower = -10., profile_upper=10., conf_level=0.95, df=1)","title":"Profile Likelihood Problem Interface"},{"category":"section","location":"rosenbrock/#Profile-Likelihood-Methods","page":"Getting Started","text":"LikelihoodProfiler provides a range of methods to profile likelihood functions and explore practical identifiability. All methods use the same solve interface but differ in how the profiles and the associated confidence intervals are computed. The solve interface exposes a set of common options shared across all profiling methods—such as parallel_type for parallel execution, verbose for controlling output, and reoptimize_init to trigger re-optimization of the initial parameter values before profiling begins.\n\nFor a more detailed description of each method, see the Profile Likelihood Methods section.","title":"Profile Likelihood Methods"},{"category":"section","location":"rosenbrock/#OptimizationProfiler","page":"Getting Started","text":"The most direct profiling method is OptimizationProfiler. It computes the profile by taking a sequence of fixed or adaptive steps in the profiled parameter and performing a re-optimization of all remaining parameters (nuisance parameters) at each step. This produces a discrete set of points that approximate the profile curve.\n\nmeth_opt = OptimizationProfiler(optimizer = LBFGSB(), stepper = AdaptiveStep())\nsol1 = solve(plprob, meth_opt)\nplot(sol1)","title":"OptimizationProfiler"},{"category":"section","location":"rosenbrock/#IntegrationProfiler","page":"Getting Started","text":"A more advanced technique is implemented in IntegrationProfiler. Instead of repeatedly re-optimizing parameters, this method internally formulates a differential equation system whose solution traces the profile likelihood trajectory. To integrate this system, the user must provide a differential equation solver (integrator).\n\nmeth_integ = IntegrationProfiler(integrator = Tsit5(), integrator_opts = (dtmax=0.3,), matrix_type = :hessian)\nsol2 = solve(plprob, meth_integ)\nplot(sol2)","title":"IntegrationProfiler"},{"category":"section","location":"rosenbrock/#CICOProfiler","page":"Getting Started","text":"Often, the primary goal of likelihood profiling is to determine whether the profile intersects the confidence threshold—i.e., whether the parameter has a finite confidence interval. CICOProfiler focuses on this directly: it estimates the confidence interval endpoints without reconstructing the full profile curve.\n\nmeth_cico = CICOProfiler(optimizer = :LN_NELDERMEAD, scan_tol = 1e-4)\nsol3 = solve(plprob, meth_cico)\nplot(sol3)","title":"CICOProfiler"},{"category":"section","location":"rosenbrock/#QuadraticApproxProfiler","page":"Getting Started","text":"QuadraticApproxProfiler builds a local quadratic approximation around the optimum from curvature information. This curvature is approximated by the Fisher Information Matrix (FIM)/Hessian. This method is fast and provides Wald-type confidence intervals without tracing the full likelihood profile.\n\nYou can inspect the Fisher Information Matrix (FIM) directly with evaluate_FIM:\n\nF = evaluate_FIM(plprob, optpars)\n\nThen solve using the quadratic-approximation method (FIM-based curvature):\n\nmeth_fim = QuadraticApproxProfiler(resolution=50)\nsol4 = solve(plprob, meth_fim)\nplot(sol4)\n\nnote: Note\nThe quadratic/FIM-based CI is a local approximation around the optimum. It may not reflect the true global likelihood shape (e.g. asymmetry or non-quadratic behavior).","title":"QuadraticApproxProfiler"},{"category":"section","location":"rosenbrock/#Profile-Likelihood-Solution","page":"Getting Started","text":"A ProfileLikelihoodSolution stores more than just the profile curves (see Solution Interface). It also contains the estimated confidence-interval endpoints and identification retcodes, which indicate whether each parameter (or function of parameters) is practically identifiable. These values can be accessed directly:\n\nretcodes(sol3)\n\nendpoints(sol3)","title":"Profile Likelihood Solution"}]
}
